# ==========================================
# NOTIFICATION SERVICE CONFIGURATION
# ==========================================
server.port=8083
spring.application.name=notification-service

# --- DATABASE (notif_db) ---
spring.datasource.url=jdbc:mysql://localhost:3306/notif_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- GMAIL CONFIGURATION ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# REPLACE THIS WITH YOUR EMAIL
spring.mail.username=wiprovatsal@gmail.com
# REPLACE THIS WITH THE 16-DIGIT APP PASSWORD (no spaces)
spring.mail.password=gniyrkthqqvqaolx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- EUREKA ---
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

# --- SWAGGER ---
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
spring.mvc.pathmatch.matching-strategy=ant_path_matcher